## 1. Localization Foundation
- [x] 1.1 建立语言资源文件与类型定义，提供获取翻译文本的工具函数或 hook。
- [x] 1.2 在 App 根部引入语言上下文，连接现有 `uiLang` 状态并保持 localStorage 持久化。

## 2. UI 接入
- [x] 2.1 替换侧边栏、头部、模式切换等核心布局中的中文静态文案为语言键值访问。
- [x] 2.2 更新工作流主要组件（提示输入、任务状态、模态框提示等）以支持动态语言切换。
- [x] 2.3 确认 toast/提示消息按当前语言显示，并在切换后即时更新。
- [x] 2.4 为“最佳实践”6 个条目和“指令模板”建立中英文映射，切换语言时匹配对应名称与内容（参考“系统提示词配置 > 自定义 System Prompt”页面的双语配置）。

## 3. Quality
- [ ] 3.1 为语言工具编写单元测试，覆盖关键翻译查找与默认回退逻辑。
- [x] 3.2 准备手动验证清单：切换语言、刷新保持状态、关键视图文本是否切换到位。
  - [x] 切换至英文后，侧边栏、头部、按钮与提示信息即时更新。
  - [x] 刷新页面后保留语言选择并持续显示对应文案。
  - [x] 在生成/编辑流程中触发 toast 与内联提示，确认语言同步切换。
